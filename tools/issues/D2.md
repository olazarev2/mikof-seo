> Задача **D2** из блока D — Измеримость и гигиена (P3).
> Полное ТЗ: https://mikof-seo-tz.vercel.app#d2 · исходник: [`docs/tz-dev-2026-09.md`](../blob/main/docs/tz-dev-2026-09.md)

- 404-страница на `/ru/*` и `/en/*` отдаётся на румынском («Pagina nu a fost gasită») — локализовать; также добавить на неё поиск и ссылки на разделы.
- `/service/999999` возвращает title «Mikof» вместо осмысленного 404-заголовка.
- Логотип в шапке на `/ru` и `/en` ведёт на румынскую главную `https://mikof.md`, а не на языковую (`/ru`, `/en`).
- В футере и шапке ссылка «Tur virtual» ведёт на `/virtualtour/index.html` — вынести на нормальный URL и добавить в sitemap.

---

### Как проверяется приёмка

```bash
node verify/verify.mjs --only D2
```

Закрывать задачу, когда эта проверка даёт **PASS**. Отчёты копятся в `verify/reports/`.